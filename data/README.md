# Dataset

This folder is used for the real and AI-generated audio datasets used to train and test the deepfake audio detection model.

The audio dataset files are not stored directly in this GitHub repository.
